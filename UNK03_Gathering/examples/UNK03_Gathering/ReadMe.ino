// คำสั่งเจอแยก >> 'l'=เลี้ยวซ้าย ,'r'=เลี้ยวขวา ,'p'=ตรง ,'s'=หยุด  , 'f' = เดินหน้า, 'c' =เดินหน้าจนถึงเซนเซอร์ใต้ท้อง
//'q' = หน้าเลี้ยวซ้าย , 'e' = หน้าเลี้ยวขวา 'b' = ถอยหลัง 'B' = ถอยหลังพ้นเส้น

// >> OK(); //
// >> OK_STOP(); //ให้หุ่นยนต์หยุด รอจนกว่าจะกดปุ่ม OK

// >> MotorStop(); //หุ่นยนต์หยุด
// >> MotorStop(100); //หุ่นยนต์หยุดตามเวลา

//ฟังก์ชันทั่วไป
////////////////////////////////// คำสั่งเคลื่อนที่ ///////////////////////////////////////////////////////////////////
// >> Motor(ความเร็วมอเตอร์ซ้าย , ความเร็วมอเตอร์ขวา); // คำสั่งเคลื่อนที่มอเตอร์
// >> Move(ความเร็วมอเตอร์ซ้าย , ความเร็วมอเตอร์ขวา, เวลาที่ใช้ ); // คำสั่งเคลื่อนที่มอเตอร์พร้อม delay เวลาที่ใช้ หน่วย 1000 = 1วินาที

// >> FD_time(ความเร็วมอเตอร์,เวลาที่ใช้); //คำสั่งเคลื่อนที่ข้างหน้า delay เวลาที่ใช้ หน่วย 1000 = 1วินาที
// >> BK_time(ความเร็ว,เวลาที่ใช้); //คำสั่งเคลื่อนที่ข้างหน้า delay เวลาที่ใช้ หน่วย 1000 = 1วินาที
// >> SR(ความเร็ว,เวลาที่ใช้);  //เลี้ยวซ้ายแบบหมุนสองล้อ delay เวลาที่ใช้ หน่วย 1000 = 1วินาที
// >> SL(ความเร็ว,เวลาที่ใช้);  //เลี้ยวขวาแบบหมุนสองล้อ delay เวลาที่ใช้ หน่วย 1000 = 1วินาที
// >> TR(ความเร็ว,เวลาที่ใช้); //เลี้ยวซ้ายแบบล้อเดียว delay เวลาที่ใช้ หน่วย 1000 = 1วินาที
// >> TL(ความเร็ว,เวลาที่ใช้);   //เลี้ยวขวาแบบล้อเดียว delay เวลาที่ใช้ หน่วย 1000 = 1วินาที

////////////////////////////////// คำสั่งเลี้ยว //////////////////////////////////////////////////////////////////
// >> SpinL();                 // หมุนหุ่นยนต์ด้านซ้าย ตามเซนเซอร์
// >> SpinL2();                // หมุนหุ่นยนต์ด้านซ้าย 2 แยก ตามเซนเซอร์
// >> SpinR();                 // หมุนหุ่นยนต์ด้านขวา ตามเซนเซอร์
// >> SpinR2();                // หมุนหุ่นยนต์ด้านขวา 2 แยก ตามเซนเซอร์
// Left(ช่องเซนเซอร์ 0->7, ความเร็วมอเตอร์); //หมนซ้ายจนกว่าเซนเซอร์ที่เลือกเจอเส้น
// Right(ช่องเซนเซอร์ 0->7, ความเร็วมอเตอร์); //หมนขวาจนกว่าเซนเซอร์ที่เลือกเจอเส้น


////////////////////////////////// คำสั่งวิ่งนับแยก //////////////////////////////////////////////////////////////////
// >> คำสั่งเจอแยก 'l' = กลางหุ่นหน้าเลี้ยวซ้าย , 'r' = กลางหุ่นหน้าเลี้ยวขวา  , 'p' = ตรง     , 's' = หยุด        , 'c' = กลางหุ่นหน้า
// >>  'q' = หน้าเลี้ยวซ้าย , 'e' = หน้าเลี้ยวขวา


// >> FF(ความเร็ว, 'คำสั่งเจอแยก');   // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ใดก็ได้
// >> FFC(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่กลาง
// >> FFL(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ซ้าย
// >> FFR(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ขวา
// >> FFL2(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ซ้าย
// >> FFR2(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ขวา
// >> FFNUM(ความเร็ว, 'คำสั่งเจอแยก', 0->7);  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >> ตำแหน่งเซนเซอร์ที่ใช้จับแยก
// >> FF_DISTANCE(ความเร็ว, 'คำสั่งเจอแยก', 4->30);  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >> ตำแหน่งเซนเซอร์วัดระยะ
// >> FFWhite(ความเร็ว, 'คำสั่งเจอแยก'); // วิ่งตามเส้น PID ไปด้านหน้าจนกว่าเซนเซอร์ไม่เจอเส้น ให้ทำตามคำสั่ง
// >> FFBlack(ความเร็ว, 'คำสั่งเจอแยก'); // // เคลื่อนที่ไปข้างหน้าจนเจอเส้น ให้ทำตามคำสั่ง
// >> FFTimer(ความเร็ว, เวลาที่ใช้); // วิ่งตามเส้น PID ตามเวลาที่ใช้

// >> MotorSensorF(ความเร็ว, 'คำสั่งเจอแยก', 0->7); //เคลื่อนที่ไปข้างหน้าหรือข้างหลังจนกว่าเซนเซอร์ที่เลือกเจอเส้น  >> ใช้เซนเซอร์ข้างหน้า
// >> MotorSensorC(ความเร็ว, 'คำสั่งเจอแยก', 0->1); //เคลื่อนที่ไปข้างหน้าหรือข้างหลังจนกว่าเซนเซอร์ที่เลือกเจอเส้น  >> ใช้เซนเซอร์ตรงกลาง


// >> _______________คำสั่ง SERVO_________________________
//servo( 1 -> 4, องศา);
//Servo(ยก 0 -> 180 ,ซ้าย 0 -> 180 ,ขวา 0 -> 180);
//ServoMove(ยก 0 -> 180 ,ซ้าย 0 -> 180 ,ขวา 0 -> 180 ,ความเร็วเซอร์โว);  //ปรับค่าเซอร์โวแบบกำหนดความเร็วการหมุน
//ServoUp(5);    // ยกขึ้น เร็ว (delay 5ms)
//ServoDown(5); // ลง ความเร็วทีละ 5 (delay 5ms)

//ServoOpen(5); //เปิดอ้า ความเร็วทีละ 5 (delay 5ms)
//ServoCloseSmall(5); //เก็บลูกเล็ก
//ServoCloseBig(5); //เก็บลูกใหญ่